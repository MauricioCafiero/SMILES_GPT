## refactor SMILES GPT. 
- This repo contains a SMILES GPT in Tensorflow.
- refactor to Pytorch. make additions to env as needed.
- set apple silicon as the default for torch, falling back to CUDA and CPU.
- build and save a foundation model using the ZN305K_smiles.csv file
- finetune using the Tyrosinase1239_IC50.csv file
- the GPT_CafChem.ipynb notebook shows the general workflow to follow.
